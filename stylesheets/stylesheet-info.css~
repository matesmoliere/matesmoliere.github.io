
/*Definimos el formato del cuerpo de la página*/
body {
  margin: 10px;
  padding: 10px;
  background: rgba(0, 0, 0, 0.9);
  color: #eaeaea; /*Color de la viñeta*/
  font-size: 18px;
  line-height: 1.2;
  font-family: Monaco, "Bitstream Vera Sans Mono", "Lucida Console", Terminal, monospace;
}

/*Hacemos que el contenido de la cabecera se comprima con max-width */
.container {
  width: 90%;
  max-width: 800px;
  margin: 0 auto;
}

section {
  display: block;
  margin: 0 0 20px 0; /*píxeles de separación de la cabecera*/
}


/*Agregamos una línea separadora para la cabecera del resto del cuerpo*/
header {
  background: url("../images/fondo-info.png") 0 0; 
  width: 100%;
  /*border-top: 2px dashed #b5e853; /*borde superior*/
  border-bottom: 2px dashed #b5e853; /*borde inferior*/
  padding: 20px 0;
  margin: 0 0 40px 0;
  
}

header h1:before {
  content: "Informática";
  font-size: 38px;
}


/* Definimos el aspecto de los encabezados*/
h1 {
  font-weight: bold;
  font-size: 28;
  font-family: Monaco, "Bitstream Vera Sans Mono", "Lucida Console", Terminal, monospace;
  color: #9e9e9e;
  letter-spacing: 0.1em;
  text-shadow: 0 1px 1px rgba(0, 0, 0, 0),
               0 0 5px rgba(181, 232, 83, 0),
               0 0 10px rgba(181, 232, 83, 0);
}


h2, h3, h4, h5, h6 {
  font-weight: normal;
  font-family: Monaco, "Bitstream Vera Sans Mono", "Lucida Console", Terminal, monospace;
  color: #b5e853;
  letter-spacing: -0.03em;
  text-shadow: 0 1px 1px rgba(0, 0, 0, 0.1),
               0 0 5px rgba(181, 232, 83, 0.1),
               0 0 10px rgba(181, 232, 83, 0.1);
}

p {

	font-family: "Verdana", Arial, sans-serif;

}

table {

   width: 100%;
   background-image:url("../images/fondotabla.png")
}

td:hover{background-color:#050505} /* Hacemos que el color cambie al pasar el cursor sobre la celda*/


/* Definimos los botones
*/

.btn {
  display: inline-block;
  /*background: -webkit-linear-gradient(top, rgba(20, 10, 240, 0.5), rgba(20, 10, 240, 0.5) 50%, rgba(20, 10, 240, 0.3) 50%, rgba(20, 10, 240, 0.5));*/
  background-color:#008cba;
  padding: 8px 18px;
  border-radius: 50px;
  border: 2px solid rgba(0, 0, 0, 0.7);
  border-bottom: 2px solid rgba(0, 0, 0, 0.7);
  border-top: 2px solid rgba(0, 0, 0, 1);
  color: rgba(255, 255, 255, 0.9);
  font-family: Helvetica, Arial, sans-serif;
  font-weight: bold;
  font-size: 16px;
  text-decoration: none;
  text-shadow: 0 -1px 0 rgba(0, 0, 0, 0.75);
  box-shadow: inset 0 1px 0 rgba(255, 255, 255, 0.05);
}

.btn:hover {
  background: -webkit-linear-gradient(top, rgba(40, 40, 40, 0.6), rgba(35, 35, 35, 0.6) 50%, rgba(10, 10, 10, 0.8) 50%, rgba(0, 0, 0, 0.8));
}

.btn .icon {
  display: inline-block;
  width: 16px;
  height: 16px;
  margin: 1px 8px 0 0;
  float: left;
}

/* Le decimos donde está el icono*/
.btn-wolfram .icon {
  opacity: 0.8;
  background: url("../images/wolfram.png") 0 0 no-repeat;
  url("http://wolframalpha.com");
}

/* Links
   a, a:hover, a:visited
*/

a {
  color: #63c0f5;
  text-shadow: 0 0 5px rgba(104, 182, 255, 0.5);
}

/* Clearfix */

.cf:before, .cf:after {
  content:"";
  display:table;
}

.cf:after {
  clear:both;
}

.cf {
  zoom:1;
}
